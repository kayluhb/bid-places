============
Installation
============

At the command line::

    $ easy_install bid-places

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bid-places
    $ pip install bid-places
